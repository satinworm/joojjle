/** @type {import('next').NextConfig} */
const nextConfig = {
   // reactStrictMode: true,
   // swcMinify: true,
   images: {
      domains: ["pyxis.nymag.com"],
   },
};

module.exports = nextConfig;
